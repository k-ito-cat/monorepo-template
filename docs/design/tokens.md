# Tokens

## この文書の役割

この文書は、デザインの決定を実装可能な値へ落としたトークンを定義するための文書である。  
ここでは、色、余白、サイズ、タイポグラフィ、角丸、境界線、重なり順、モーションを扱う。  

## 運用ルール

- 上位原則は `docs/design/DESIGN.md` を正本とする
- foundations の基礎ルールは `docs/design/foundations.md` を正本とする
- この文書は実装で参照する値を扱う

## 色

- `color.background.default`:
- `color.surface.default`:
- `color.text.primary`:
- `color.text.secondary`:
- `color.border.default`:
- `color.accent.primary`:
- `color.success.default`:
- `color.warning.default`:
- `color.danger.default`:

## 余白

- `space.1`:
- `space.2`:
- `space.3`:
- `space.4`:
- `space.5`:
- `space.6`:

## タイポグラフィ

- `font.family.heading`:
- `font.family.body`:
- `font.size.caption`:
- `font.size.body`:
- `font.size.subheading`:
- `font.size.heading`:

## 角丸

- `radius.sm`:
- `radius.md`:
- `radius.lg`:

## 境界線

- `border.width.default`:
- `border.width.strong`:

## サイズ

- `size.input.height`:
- `size.button.height`:
- `size.thumbnail.width`:
- `size.thumbnail.height`:

## 重なり順

- `z.modal`:
- `z.sheet`:
- `z.dropdown`:
- `z.toast`:

## モーション

- `motion.duration.fast`:
- `motion.duration.base`:
- `motion.easing.standard`:
